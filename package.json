{
  "name": "chaijohn-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv CHAIJOHN_KV",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "node-fetch": "^3.3.2",
    "dotenv": "^16.0.0"
  }
}
